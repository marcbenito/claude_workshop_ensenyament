# Connexió a la base de dades Postgres (vegeu bd/ per a l'esquema)
DATABASE_URL=postgresql://usuari:contrasenya@localhost:5432/reserva_sessions

# Secret de NextAuth per xifrar sessions/JWT. Obligatori en producció.
# Genera'n un amb: npx auth secret
AUTH_SECRET=
